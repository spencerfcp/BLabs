
import { formatTimestamp } from 'ember-serve/helpers/format-timestamp';
import { module, test } from 'qunit';

module('Unit | Helper | format timestamp');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatTimestamp([42]);
  assert.ok(result);
});

